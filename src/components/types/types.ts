export interface Stat {
    title: string;
    img: string;
    alt: string,
    description: string;
}

export type Stats = Stat[];

export interface Bullet {
    img: string,
    alt: string,
    title: string,
}